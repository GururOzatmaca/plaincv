// True A4 in CSS px at 96dpi. Shared so the editor paper and the template
// thumbnails scale from one definition.
export const A4_W = (210 * 96) / 25.4; // 793.7
export const A4_H = (297 * 96) / 25.4; // 1122.5
